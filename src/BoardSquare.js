class BoardSquare {
    constructor(isWhiteSquare, row, col, currentPiece) {
        this.isWhiteSquare = isWhiteSquare;
		this.row = row;
		this.col = col;
		this.currentPiece = currentPiece;
    }
}